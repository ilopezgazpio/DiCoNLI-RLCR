# Shared dependencies for full fine-tuning, LoRA/QLoRA, inference, evaluation,
# and tests. Use Python 3.10 on Linux x86_64; see README.md for venv setup.
# Install the CUDA 12.4 PyTorch wheel first, before building DeepSpeed.
torch==2.5.1
numpy==1.26.4

# Training and distributed execution
transformers==4.48.3
accelerate==1.3.0
deepspeed==0.17.0
datasets==4.0.0

# Adapter training and 4-bit quantization
peft==0.14.0
bitsandbytes==0.45.5

# The custom trainer uses helpers from this specific TRL revision.
trl @ git+https://github.com/huggingface/trl.git@69ad852e5654a77f1695eb4c608906fe0c7e8624

# Tokenization, rewards, evaluation, and logging
sentencepiece==0.2.0
tiktoken==0.9.0
pandas==2.2.3
tqdm==4.67.1
pyyaml==6.0.2
packaging==25.0
wandb==0.19.6
pytest==8.4.0
